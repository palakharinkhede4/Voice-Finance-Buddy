streamlit>=1.40.0
torch
torchvision
transformers
openai>=1.54.0
python-dotenv>=1.0.0
plotly>=5.22.0
pandas>=2.0.0
audio-recorder-streamlit>=0.0.10
pydub>=0.25.1
soundfile>=0.12.1
numpy>=1.24.0
requests>=2.31.0

# Stage 1 — VAD (Silero, ONNX-based, no torch required)
silero-vad>=5.1.2
onnxruntime>=1.17.0

# Stage 2 — STT (faster-whisper local model)
faster-whisper>=1.0.0

# Stage 5 — RAG (FAISS + Sentence Transformers dense embeddings)
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# Real Finance APIs (Stage 6 tools — live NSE/BSE/MF data)
yfinance>=0.2.40

# Free TTS
gTTS>=2.5.0
